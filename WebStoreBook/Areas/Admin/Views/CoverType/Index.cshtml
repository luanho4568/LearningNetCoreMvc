@model IEnumerable<CoverType>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2>Cover Type List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus"></i>
                Create new Cover Type
            </a>
        </div>
    </div>
    <table class="table table-bordered table-striped mt-4">
        <thead>
        <th>Name</th>
        <th>Actions</th>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>
                        <a asp-controller="CoverType" asp-action="Edit" asp-route-id=@item.Id class="btn btn-warning">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a asp-controller="CoverType" asp-action="Delete" asp-route-id=@item.Id class="btn btn-danger">
                            <i class="bi bi-trash"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
